# UX Фидбек и План Исправлений

> Анализ продукта с точки зрения владельца бизнеса (не-технический пользователь)
> Дата: 2025-12-30

---

## Часть 1: Обнаруженные Проблемы

### 1.1 Критические (блокируют использование)

#### P1-CRIT-01: "Полный анализ" не показывает уточняющие вопросы

**Корневая причина найдена!**

При Full Analysis, если код-анализ возвращает `needs_clarification: true`:
1. `codeData.analysis` = null
2. Условие `if (businessData.canvas && codeData.analysis)` НЕ выполняется
3. Gap Analysis НЕ вызывается
4. Результат: показывается только Business Canvas

**Код проблемы (page.tsx:437):**
```typescript
if (businessData.canvas && codeData.analysis) {
  // Gap analysis не вызывается если codeData.analysis = null!
  const gapResponse = await fetch('/api/analyze-gaps', ...);
}
```

**Что видит пользователь:**
- Только Business Canvas
- НЕ видит вопросы-уточнения от LLM
- НЕ видит причину почему нет анализа кода

**Что должен видеть:**
- Business Canvas (готов)
- Секцию "⚠️ Требуется уточнение" с вопросами LLM
- Возможность ответить и продолжить анализ

**Impact:** CRITICAL — пользователь не понимает что происходит

#### P1-CRIT-02: Неочевидная валидация формы
- **Проблема:** Кнопка "Запустить" заблокирована без объяснения почему
- **Причина:** Требуется заполнить "Опишите свой проект" в Шаге 2
- **Impact:** Пользователь думает что сервис сломан

---

### 1.2 Высокий приоритет (ухудшают опыт)

#### P2-HIGH-01: Технический жаргон на главной странице

| Где | Термин | Проблема |
|-----|--------|----------|
| Карточка "Разбор бизнеса" | "Business Model Canvas — 9 блоков" | Не объяснено что это за блоки |
| Карточка "Полный анализ" | "Gap Detection" | Английский, непонятно |
| Карточка "Проверка кода" | "Репозиторий" | Технический термин |
| Карточка "Проверка кода" | "Технологический стек" | IT-жаргон |
| Карточка "Конкуренты" | "Парсинг сайтов" | Непонятно что это |

#### P2-HIGH-02: Английские термины в результатах
- "Early Traction" вместо "🌱 Ранняя стадия"
- "Key Partners", "Key Activities" и т.д. (дублируются, но основной текст на русском — ОК)

#### P2-HIGH-03: Технические данные в UI
- "Бизнес: 29087ms" — пользователю это ничего не говорит
- Должно быть: "Анализ занял ~30 сек" или скрыть совсем

---

### 1.3 Средний приоритет (можно улучшить)

#### P3-MED-01: Нет действий после анализа
- Нет кнопки экспорта (PDF, Markdown)
- Нет возможности сохранить результат
- Нет чата для уточняющих вопросов (как в режиме "Проверка кода")

#### P3-MED-02: Нет Alignment Score в результатах
- Объяснён в FAQ
- Не показан нигде в результатах
- Пользователь ожидает увидеть цифру

#### P3-MED-03: Нет онбординга для новых пользователей
- Что такое Business Canvas?
- Как правильно описать бизнес?
- Зачем нужен GitHub URL?

---

### 1.4 Низкий приоритет (nice to have)

#### P4-LOW-01: Нет примеров заполнения
- Кнопка "Показать пример" рядом с полями
- Шаблон для описания бизнеса

#### P4-LOW-02: Нет интеграций
- Экспорт задач в Notion/Trello
- Поделиться результатом по ссылке

---

## Часть 2: План Исправлений

### Фаза 1: Критические исправления (ASAP)

#### Task 1.1: Показать все результаты в "Полном анализе"

**Текущее состояние:**
- `page.tsx` рендерит только `businessResult` в Full Analysis mode
- `codeResult`, `gapResult`, `competitorResult` есть, но не отображаются

**Что нужно сделать:**

```
src/app/page.tsx
├── Добавить табы для всех результатов:
│   ├── 📊 Бизнес (BusinessCanvas)
│   ├── 💻 Код (CodeAnalysis)
│   ├── 🎯 Разрывы (GapDetection)
│   ├── 🏆 Конкуренты (если есть)
│   └── ✅ Задачи на неделю
├── Показать Alignment Score сверху
└── Добавить сводку результатов
```

**Подзадачи:**
- [ ] 1.1.1: Создать компонент `FullAnalysisResults.tsx`
- [ ] 1.1.2: Добавить табы с переключением
- [ ] 1.1.3: Интегрировать существующие компоненты результатов
- [ ] 1.1.4: Добавить Alignment Score badge
- [ ] 1.1.5: Добавить сводку "Что нашли" сверху

**Файлы:**
- `src/app/page.tsx` — интеграция нового компонента
- `src/components/analysis/FullAnalysisResults.tsx` — NEW
- `src/components/analysis/ResultsTabs.tsx` — NEW
- `src/components/analysis/AlignmentScoreBadge.tsx` — NEW

---

#### Task 1.2: Исправить валидацию формы

**Текущее состояние:**
```typescript
// page.tsx:571-582
const canSubmit =
  analysisMode === 'full'
    ? businessInput.description.length >= 50 &&
      (repoUrl || uploadedFiles.length > 0) &&
      description.trim()  // <-- это "Опишите свой проект"
    : ...
```

**Проблема:** `description.trim()` требуется, но пользователь не видит что поле обязательно

**Что нужно сделать:**
- [ ] 1.2.1: Добавить визуальную индикацию обязательных полей (красная звёздочка)
- [ ] 1.2.2: Добавить текст ошибки под незаполненными полями
- [ ] 1.2.3: Подсветить невалидные поля при попытке отправки
- [ ] 1.2.4: Показать tooltip на заблокированной кнопке с причиной

**Файлы:**
- `src/app/page.tsx` — добавить состояние валидации
- `src/components/forms/ValidatedInput.tsx` — NEW (или расширить существующие)

---

### Фаза 2: Улучшение понятности (1-2 дня)

#### Task 2.1: Заменить технический жаргон

**Таблица замен:**

| Было | Стало |
|------|-------|
| "Business Model Canvas — 9 блоков" | "Карта бизнеса — кто клиенты, как зарабатываете, что делаете" |
| "Gap Detection" | "Поиск слабых мест" |
| "Репозиторий" | "Код проекта" |
| "Покажите репозиторий" | "Укажите ссылку на код (GitHub)" |
| "Технологический стек" | "Используемые технологии" |
| "Парсинг сайтов" | "Автоматический сбор информации" |
| "Early Traction" | "🌱 Ранняя стадия" |
| "Бизнес: 29087ms" | скрыть или "~30 сек" |

**Подзадачи:**
- [ ] 2.1.1: Обновить текст на главной странице
- [ ] 2.1.2: Обновить лейблы в формах
- [ ] 2.1.3: Обновить отображение stage в результатах
- [ ] 2.1.4: Убрать/форматировать технические тайминги

**Файлы:**
- `src/app/page.tsx` — тексты карточек
- `src/components/analysis/AnalysisModeSelector.tsx` — тексты режимов
- `src/components/results/BusinessCanvasDisplay.tsx` — stage names

---

#### Task 2.2: Добавить тултипы для терминов

**Термины для тултипов (в дополнение к существующим):**
- Business Canvas (на главной)
- Alignment Score (в результатах)
- Gap / Разрыв (в результатах)
- Каждый блок Canvas (Key Partners, Value Proposition, etc.)

**Подзадачи:**
- [ ] 2.2.1: Расширить dictionary.ts новыми терминами
- [ ] 2.2.2: Добавить TermTooltip к заголовкам блоков Canvas
- [ ] 2.2.3: Добавить InfoTooltip к Alignment Score

**Файлы:**
- `src/lib/tooltips/dictionary.ts`
- `src/components/results/BusinessCanvasDisplay.tsx`

---

### Фаза 3: Дополнительные улучшения (3-5 дней)

#### Task 3.1: Добавить экспорт результатов

**Функционал:**
- Кнопка "📥 Скачать отчёт"
- Форматы: Markdown, JSON
- Содержит все результаты анализа

**Подзадачи:**
- [ ] 3.1.1: Создать функцию `exportToMarkdown(results)`
- [ ] 3.1.2: Создать функцию `exportToJSON(results)`
- [ ] 3.1.3: Добавить кнопку экспорта в UI
- [ ] 3.1.4: Реализовать скачивание файла

**Файлы:**
- `src/lib/export/markdown.ts` — NEW
- `src/lib/export/json.ts` — NEW
- `src/components/results/ExportButton.tsx` — NEW

---

#### Task 3.2: Добавить follow-up чат для Full Analysis

**Текущее состояние:**
- Чат есть только в режиме "Проверка кода"
- В Full Analysis чата нет

**Что нужно:**
- Чат с контекстом всех результатов (бизнес + код + gaps)
- Возможность задать уточняющие вопросы

**Подзадачи:**
- [ ] 3.2.1: Расширить API `/api/chat/stream` для Full Analysis контекста
- [ ] 3.2.2: Добавить компонент чата в FullAnalysisResults
- [ ] 3.2.3: Передавать все результаты как контекст

**Файлы:**
- `src/app/api/chat/stream/route.ts`
- `src/components/chat/AnalysisChat.tsx`

---

#### Task 3.3: Добавить сохранение в проект

**Текущее состояние:**
- Есть чекбокс "Сохранить в проект"
- Работает для Code Analysis
- Не работает полноценно для Full Analysis

**Подзадачи:**
- [ ] 3.3.1: Проверить сохранение всех типов результатов
- [ ] 3.3.2: Добавить отображение сохранённых Full Analysis в проекте
- [ ] 3.3.3: Добавить историю анализов

---

## Часть 3: Приоритезация

### Спринт 1 (Критические — делаем первыми)

| # | Задача | Effort | Impact |
|---|--------|--------|--------|
| 1 | Task 1.1: Показать все результаты Full Analysis | 4-6h | Critical |
| 2 | Task 1.2: Исправить валидацию формы | 2h | High |

**Итого: ~6-8 часов**

### Спринт 2 (Понятность)

| # | Задача | Effort | Impact |
|---|--------|--------|--------|
| 3 | Task 2.1: Заменить жаргон | 2h | High |
| 4 | Task 2.2: Добавить тултипы | 2h | Medium |

**Итого: ~4 часа**

### Спринт 3 (Улучшения)

| # | Задача | Effort | Impact |
|---|--------|--------|--------|
| 5 | Task 3.1: Экспорт результатов | 3h | Medium |
| 6 | Task 3.2: Follow-up чат | 4h | Medium |
| 7 | Task 3.3: Сохранение в проект | 2h | Low |

**Итого: ~9 часов**

---

## Часть 4: Детальный План Task 1.1

> Это самая важная задача — корректно обрабатывать все сценарии Full Analysis

### 4.0 Сценарии Full Analysis

```
Сценарий A: Всё успешно
├── businessData.canvas ✓
├── codeData.analysis ✓
├── gapResult ✓
└── Показать: Canvas + Code + Gaps + Tasks

Сценарий B: Код требует уточнения (ТЕКУЩИЙ БАГ)
├── businessData.canvas ✓
├── codeData.needs_clarification = true
├── codeData.questions ✓
├── codeData.analysis = null
├── gapResult = null (не вызывался!)
└── Показать: Canvas + Вопросы уточнения + кнопка "Продолжить"

Сценарий C: Ошибка в одном из анализов
├── businessData.success = false OR codeData.success = false
└── Показать: Ошибку + что получилось
```

### 4.1 Текущая структура результатов

```typescript
// page.tsx state
const [codeResult, setCodeResult] = useState<CodeAnalysisResult | null>(null);
const [businessResult, setBusinessResult] = useState<BusinessAnalysisResult | null>(null);
const [gapResult, setGapResult] = useState<GapAnalysisResult | null>(null);
const [competitorResult, setCompetitorResult] = useState<CompetitorAnalysisResult | null>(null);
```

### 4.2 Что показывается сейчас (Full Analysis)

```tsx
// page.tsx ~line 870
{analysisMode === 'full' && hasBusinessResult && (
  <ResultsAccordion title="📊 Business Canvas" defaultOpen>
    <BusinessCanvasDisplay result={businessResult} />
  </ResultsAccordion>
)}
```

**Проблема:** Только Business Canvas, остальное игнорируется

### 4.3 Что должно показываться

```
┌─────────────────────────────────────────────────────────┐
│  Alignment Score: 72/100  🟡 ITERATE                    │
│  "Есть хорошая база, но нужны доработки"                │
├─────────────────────────────────────────────────────────┤
│  [📊 Бизнес] [💻 Код] [🎯 Разрывы] [🏆 Конкуренты]     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  <Контент активной вкладки>                             │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  ✅ Задачи на неделю (5)                                │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 1. Добавить Stripe Checkout (4h) 🔴              │   │
│  │ 2. Настроить аналитику Mixpanel (2h) 🟡          │   │
│  │ ...                                              │   │
│  └──────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  [📥 Скачать отчёт]  [💬 Задать вопрос]                │
└─────────────────────────────────────────────────────────┘
```

### 4.4 Компоненты для создания

```
src/components/analysis/
├── FullAnalysisResults.tsx      # Главный контейнер
├── AlignmentScoreBadge.tsx      # Бейдж со скором
├── ResultsTabs.tsx              # Табы переключения
├── WeeklyTasksList.tsx          # Список задач на неделю
└── ResultsActions.tsx           # Кнопки действий
```

### 4.5 Структура FullAnalysisResults.tsx

```tsx
interface FullAnalysisResultsProps {
  businessResult: BusinessAnalysisResult | null;
  codeResult: CodeAnalysisResult | null;
  gapResult: GapAnalysisResult | null;
  competitorResult: CompetitorAnalysisResult | null;
}

export function FullAnalysisResults({
  businessResult,
  codeResult,
  gapResult,
  competitorResult,
}: FullAnalysisResultsProps) {
  const [activeTab, setActiveTab] = useState<'business' | 'code' | 'gaps' | 'competitors'>('business');

  // Собираем alignment score из gap result
  const alignmentScore = gapResult?.alignment_score ?? null;

  // Собираем задачи из всех источников
  const weeklyTasks = [
    ...(gapResult?.tasks ?? []),
    ...(codeResult?.tasks ?? []),
  ];

  return (
    <div className="full-analysis-results">
      {/* Alignment Score */}
      {alignmentScore && (
        <AlignmentScoreBadge score={alignmentScore} />
      )}

      {/* Tabs */}
      <ResultsTabs
        activeTab={activeTab}
        onTabChange={setActiveTab}
        hasCode={!!codeResult}
        hasBusiness={!!businessResult}
        hasGaps={!!gapResult}
        hasCompetitors={!!competitorResult}
      />

      {/* Tab Content */}
      <div className="tab-content">
        {activeTab === 'business' && businessResult && (
          <BusinessCanvasDisplay result={businessResult} />
        )}
        {activeTab === 'code' && codeResult && (
          <CodeAnalysisDisplay result={codeResult} />
        )}
        {activeTab === 'gaps' && gapResult && (
          <GapAnalysisDisplay result={gapResult} />
        )}
        {activeTab === 'competitors' && competitorResult && (
          <CompetitorAnalysisDisplay result={competitorResult} />
        )}
      </div>

      {/* Weekly Tasks */}
      {weeklyTasks.length > 0 && (
        <WeeklyTasksList tasks={weeklyTasks} />
      )}

      {/* Actions */}
      <ResultsActions
        onExport={() => {/* ... */}}
        onChat={() => {/* ... */}}
      />
    </div>
  );
}
```

### 4.6 Исправление Сценария B (Приоритет: КРИТИЧЕСКИЙ)

**Проблема:** При `codeData.needs_clarification = true` пользователь не видит вопросы

**Решение:**

```tsx
// page.tsx - после получения результатов Full Analysis

// В секции рендеринга результатов Full Analysis (строка ~1026):
{analysisMode === 'full' && (
  <div className="results full-results">
    {/* Показать Business Canvas если есть */}
    {businessResult?.canvas && (
      <details className="results-section" open>
        <summary>📊 Business Canvas</summary>
        <CanvasView ... />
      </details>
    )}

    {/* НОВОЕ: Показать вопросы уточнения если код требует их */}
    {codeResult?.needs_clarification && codeResult?.questions && (
      <div className="clarification-section">
        <h3>⚠️ Требуется уточнение по коду</h3>
        <p>Для полного анализа нужно ответить на вопросы:</p>
        <ClarificationQuestions
          questions={codeResult.questions}
          onSubmit={handleFullAnalysisClarification}
          disabled={loading}
        />
      </div>
    )}

    {/* Gap результаты если есть */}
    {gapResult && (
      <>
        <VerdictBadge ... />
        <AlignmentScore ... />
        <GapsView ... />
      </>
    )}

    {/* Анализ кода если есть полный результат */}
    {codeResult?.analysis && (
      <details className="results-section">
        <summary>💻 Анализ кода</summary>
        <AnalysisView ... />
      </details>
    )}
  </div>
)}
```

**Новый хендлер для ответа на уточнения в Full Analysis:**

```tsx
// page.tsx - новая функция

const handleFullAnalysisClarification = async (answers: Record<string, string>) => {
  // 1. Обновить описание проекта с ответами
  const answersText = Object.entries(answers)
    .map(([, answer]) => `\n\n[Уточнение: ${answer}]`)
    .join('');

  const updatedDescription = description + answersText;
  setDescription(updatedDescription);

  // 2. Перезапустить ТОЛЬКО анализ кода с обновлённым описанием
  setLoading(true);
  const codeResponse = await fetch('/api/analyze', {
    method: 'POST',
    body: JSON.stringify({
      repo_url: repoUrl,
      files: uploadedFiles,
      project_description: updatedDescription,
    }),
  });
  const codeData = await codeResponse.json();
  setPersistedResult(codeData);

  // 3. Если теперь есть analysis — запустить gap detection
  if (codeData.analysis && businessResult?.canvas) {
    const gapResponse = await fetch('/api/analyze-gaps', {
      method: 'POST',
      body: JSON.stringify({
        canvas: businessResult.canvas,
        code_analysis: codeData.analysis,
      }),
    });
    const gapData = await gapResponse.json();
    setGapResult(gapData);
  }

  setLoading(false);
};
```

### 4.7 Файлы для изменения

| Файл | Действие | Описание |
|------|----------|----------|
| `src/app/page.tsx` | MODIFY | Добавить отображение вопросов в Full Analysis |
| `src/app/page.tsx` | MODIFY | Добавить `handleFullAnalysisClarification` |
| `src/components/analysis/FullAnalysisResults.tsx` | CREATE (optional) | Вынести логику в компонент |
| `src/components/analysis/AlignmentScoreBadge.tsx` | CREATE | Бейдж скора |
| `src/components/analysis/ResultsTabs.tsx` | CREATE | Табы |
| `src/components/analysis/WeeklyTasksList.tsx` | CREATE | Задачи |
| `src/components/analysis/ResultsActions.tsx` | CREATE | Кнопки |

---

## Часть 5: Чеклист для Тестирования

После реализации проверить:

### Full Analysis Flow — Сценарий A (успешный)
- [ ] Заполнить все поля формы (свой репозиторий + описание бизнеса)
- [ ] Кнопка активируется
- [ ] Анализ запускается
- [ ] Показывается прогресс
- [ ] Результаты отображаются: Business Canvas
- [ ] Результаты отображаются: Code Analysis
- [ ] Результаты отображаются: Gap Detection
- [ ] Результаты отображаются: Competitors (если добавлены)
- [ ] Alignment Score виден
- [ ] Задачи на неделю отображаются
- [ ] Табы/секции переключаются корректно

### Full Analysis Flow — Сценарий B (требуется уточнение)
- [ ] Использовать несоответствующий репо (shadcn/ui + описание фитнес-бизнеса)
- [ ] Анализ запускается
- [ ] Business Canvas отображается
- [ ] Вопросы уточнения отображаются (секция "⚠️ Требуется уточнение")
- [ ] Можно ответить на вопросы
- [ ] После ответа — анализ продолжается
- [ ] Gap Detection выполняется после уточнения

### Валидация формы
- [ ] Обязательные поля помечены звёздочкой
- [ ] При пустом поле показывается ошибка
- [ ] Кнопка показывает tooltip с причиной блокировки

### Терминология
- [ ] Нет "Gap Detection" — есть "Поиск слабых мест"
- [ ] Нет "Repository" — есть "Код проекта"
- [ ] Нет "Early Traction" — есть "🌱 Ранняя стадия"
- [ ] Нет "29087ms" — скрыто или форматировано

---

## Метрики Успеха

После исправлений:

| Метрика | До | После (цель) |
|---------|-----|--------------|
| Понятность UI | 7/10 | 9/10 |
| Полезность результатов | 8/10 | 9/10 |
| Полнота анализа | 5/10 | 9/10 |
| Actionability | 6/10 | 8/10 |
| **Общее** | **6.5/10** | **8.5/10** |
